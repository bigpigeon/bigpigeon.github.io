version: '2'
services:
  generate:
    image: bigpigeon0/blog_temp
    build: docker/hugo
    volumes:
      - .:/data
    working_dir: /data
    command: hugo -b https://bigpigeon.org
  sandbox:
    image: sandbox
    command: sandbox
    restart: always
